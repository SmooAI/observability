# @smooai/observability-next

## 0.1.0

### Minor Changes

- Initial release. `withSmooObservability(nextConfig)` build-time wrapper (enables `productionBrowserSourceMaps`, scaffolds source-map upload) and `<RootErrorBoundary>` drop-in for `app/global-error.tsx` / `app/error.tsx`.
