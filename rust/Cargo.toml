[workspace]
# SMOODEV-1292: api-prime moved to SmooAI/smooai monorepo (rust/api-prime/).
# Only the observability log viewer crate remains here.
members = ["observability"]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
